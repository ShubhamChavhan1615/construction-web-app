<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Project</title>

    <!-- Tailwind CSS CDN (use this for quick setup, but you might prefer using a build tool for production) -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <div class="container mx-auto mt-10">
        <h2 class="text-2xl font-semibold text-center mb-6 text-gray-800">Edit Project</h2>
        
        <div class="max-w-lg mx-auto bg-white p-6 rounded-lg shadow-lg">
            <!-- Form to update project details -->
            <form action="/api-project/update/<%= project._id %>" method="POST">
                <!-- Hidden _method field to override the form method to PUT -->
                <input type="hidden" name="_method" value="PUT">

                <!-- Project Title -->
                <div class="mb-4">
                    <label for="title" class="block text-sm font-medium text-gray-700">Project Title</label>
                    <input type="text" id="title" name="title" value="<%= project.title %>" 
                        class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500" required>
                </div>

                <!-- Project Description -->
                <div class="mb-4">
                    <label for="description" class="block text-sm font-medium text-gray-700">Project Description</label>
                    <textarea id="description" name="description" rows="4" 
                        class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500" required><%= project.description %></textarea>
                </div>

                <!-- Project Image -->
                <div class="mb-4">
                    <label for="image" class="block text-sm font-medium text-gray-700">Project Image URL</label>
                    <input type="text" id="image" name="image" value="<%= project.image %>" 
                        class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500">
                    
                    <% if (project.image) { %>
                        <p class="mt-2 text-sm text-gray-500">Current Image:</p>
                        <img src="<%= project.image %>" alt="<%= project.title %>" class="mt-2 rounded-lg w-32">
                    <% } %>
                </div>

                <!-- Submit Button -->
                <div class="flex justify-center">
                    <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition duration-300">
                        Update Project
                    </button>
                </div>
            </form>
        </div>
    </div>
    
</body>
</html>
